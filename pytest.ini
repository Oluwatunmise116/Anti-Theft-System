[pytest]
testpaths = tests
markers =
    real_models: loads real trained weights. Skipped unless --run-real-models
        is passed, so a normal `pytest` run never depends on a checkpoint
        existing and never spends minutes in CPU forward passes.
